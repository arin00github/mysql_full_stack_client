import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button, ButtonProps } from './Button';

<Meta title="MDX/Button" component={Button} />

export const Template = (args) => <Button {...args} />;

# Button

Let's define a story for our 'Button' component:

<Story name="My Button" args={{ children: 'I am in MDX' }}>
  {Template.bind({})}
</Story>

<ArgsTable story="My Button" />

I want extra description here!!

<Canvas>
  <Story name="Secondary" args={{ children: 'MDX secondary', variant: 'secondary' }}>
    {Template.bind({})}
  </Story>
</Canvas>
